import { RideDataTypeOutput } from './ride-data-type.output';

export type RideTypeOutput = {
  data: RideDataTypeOutput;
};

// ? RideTypeOutput - response to client (те що ми відправляємо клієнту).
