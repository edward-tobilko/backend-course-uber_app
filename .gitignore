# =========================
# Node.js / TypeScript ignore
# =========================

# Dependencies
node_modules/

# Build outputs
dist/
build/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Coverage (tests)
coverage/

# Temp files
*.tmp
*.swp
*.swo

# If needed
### .yarn.lock
### .package-lock.json
### .pnpm-lock.yaml

# node_modules/ — важкий каталог із залежностями (ніколи не пушать).
# dist/, build/ — збірка, яку можна відновити через npm run build.
# .env* — конфіденційні змінні (ключі API, паролі, токени).
# .tsbuildinfo — кеш TypeScript, створюється при інкрементальній компіляції.
# .vscode/, .idea/ — налаштування IDE, індивідуальні для кожного розробника.
# coverage/ — результати тестів (Jest, Mocha тощо).
# .log, *.tmp, *.swp — тимчасові та дебаг-файли.
# .src/pages/orders.html - якщо ми хочемо, щоб якийсь файл не відстежувався та не потрапляв на github.
